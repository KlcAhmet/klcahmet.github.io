import { Col, Row } from 'react-bootstrap';

const Startpage = () => {
    return (
        <Row className="startpage" style={{ padding: 0 }}>
            <Col className="startpage-up" xs={12}>lor</Col>
            <Col className="startpage-down" xs={12}>lor</Col>
        </Row>
    )
}

export default Startpage